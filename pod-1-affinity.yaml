apiVersion: v1
kind: Pod
metadata:
  name: pod-1
  labels:
    app: pod-1-label
  namespace: ns-1
spec:
  affinity:
    podAntiAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
      - labelSelector:
          matchExpressions:
          - key: app
            operator: In
            values:
            - pod-1-label # Label used by pods in ns-1
        topologyKey: kubernetes.io/hostname
  containers:
  - name: nginx
    image: nginx
